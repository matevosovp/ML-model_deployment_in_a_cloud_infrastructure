global:
  scrape_interval: 5s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "ml_service"
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets: ["ml_service:8000"]
        labels:
          service: "real-estate-ml"
